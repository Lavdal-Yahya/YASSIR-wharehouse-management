# Dependencies
node_modules/

# Build output
dist/
build/
.vite/
coverage/
*.tsbuildinfo

# Env
.env
.env.*
!.env.example

# Uploads (product images, etc.) — anchored so it doesn't accidentally match
# source folders named "uploads" (e.g. api/src/common/uploads/).
/uploads/
/api/uploads/

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/settings.json.example
*.log

# Prisma
api/prisma/*.db
api/prisma/*.db-journal
