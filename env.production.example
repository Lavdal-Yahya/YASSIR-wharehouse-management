# Production environment — copy this file to .env, then start:
#   cp env.production.example .env
#   docker compose -f docker-compose.prod.yml up -d --build
#
# All values below are pre-filled for warehouse.cliniq.live.
# Never commit the real .env file. It is listed in .gitignore.

# ---------------------------------------------------------------------------
# Public domain (no trailing slash, no protocol — Caddy adds https://)
# ---------------------------------------------------------------------------
DOMAIN=warehouse.cliniq.live

# ---------------------------------------------------------------------------
# PostgreSQL
# ---------------------------------------------------------------------------
DB_USER=app
DB_PASSWORD=kD7ub0eksLwYYP6VqXca
DB_NAME=shopdb

# ---------------------------------------------------------------------------
# API
# ---------------------------------------------------------------------------
WEB_ORIGIN=https://warehouse.cliniq.live

SESSION_SECRET=61c0c5ad02b513dcbab9b8d5a8c3c486df3852e8e3d5fbfa7082fe37646d08f91510fd66d2937885999ef841ad23fdc24173afc60a7cf1f1b06c10e3f072b587

# ---------------------------------------------------------------------------
# Seed admin (created on first deploy — change the password after first login)
# ---------------------------------------------------------------------------
SEED_ADMIN_USERNAME=admin
SEED_ADMIN_PASSWORD=y9q7mpkJ7gGTr4zzzE1O
